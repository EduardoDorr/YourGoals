namespace YourGoals.Application.Shared.Models;

public static class ModelsExtension
{
    //public static IEnumerable<TResult> ToViewModel<TEntity, TResult>(this IEnumerable<TEntity> transactions) where TEntity : BaseEntity
    //{
    //    return transactions is not null
    //         ? transactions.Select(fg => fg.ToViewModel()).ToList()
    //         : [];
    //}
}